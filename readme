The inputfile must end with ".txt" format,and the output value will be e-5 decimal accuracy.
The command should be like:
python yj2199lab3.py -df .9 -min -iter 50 -tol 0.0001 input1.txt
For example,the input file is "input1.txt",with -df=0.9, with iters,tolerance,policy are all default,just neglect them.Using command:
python yj2199lab3.py -df .9  input1.txt